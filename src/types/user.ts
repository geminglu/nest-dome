export enum UserRole {
  ADMIN = '0',
  USER = '1',
}

export enum Gender {
  FEMALE = '0',
  MALE = '1',
}

export enum Active {
  DISABLE = '0',
  ENABLE = '1',
}

export enum SystemMenu {
  DIRECTORY = 'directory',
  MENU = 'menu',
}

export enum SystemMenuHidden {
  YES = '0',
  NO = '1',
}
